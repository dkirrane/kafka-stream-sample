spring:
  application:
    name: kstream-sample
server:
  port: 8081
logging:
  pattern:
    console: "%d{yyyy-MM-dd} | %d{HH:mm:ss.SSS} | %5level | %-100.100thread | %-60.60logger{60} | %m%n"
    file: "%d{yyyy-MM-dd} | %d{HH:mm:ss.SSS} | %5level | %-100.100thread | %-60.60logger{60} | %m%n"
  file:
    name: ./logs/${spring.application.name}-${server.port}.log

---
spring:
  config:
    activate:
      on-profile: dev

kafka:
  serviceUri: public-kstreams-issue-avaya-d337.aivencloud.com:25624
  username: avnadmin
  password: AVNS_QgUtBa36nIKSY2WGXS-
  schemaRegistryUri: https://avnadmin:AVNS_QgUtBa36nIKSY2WGXS-@public-kstreams-issue-avaya-d337.aivencloud.com:25615

app:
  inputTopic: input-topic
  outputTopic: output-topic